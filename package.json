{
  "name": "drozq-com",
  "private": true,
  "type": "module",
  "workspaces": [
    "cloudflare/dashboard-worker"
  ],
  "scripts": {
    "test": "npm run dashboard:test",
    "typecheck": "npm run dashboard:typecheck",
    "build": "npm run dashboard:build",
    "dashboard:test": "npm run test --workspace @drozq/dashboard-worker && node scripts/test_dashboard_static.mjs",
    "dashboard:typecheck": "npm run typecheck --workspace @drozq/dashboard-worker",
    "dashboard:build": "npm run build --workspace @drozq/dashboard-worker",
    "dashboard:check": "npm run check --workspace @drozq/dashboard-worker && node scripts/test_dashboard_static.mjs",
    "dashboard:list-google-ads-actions": "npm run list-google-ads-actions --workspace @drozq/dashboard-worker"
  }
}
